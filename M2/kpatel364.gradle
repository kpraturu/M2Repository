apply plugin: 'java'

defaultTasks 'jar'

jar {
    manifest {
    }
}

dependencies {
    compile files('lib/resources.jar')
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

compileJava {}
